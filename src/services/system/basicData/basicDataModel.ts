export interface SystemAreaOptionsType {
  id: string;
  name: string;
  code: string;
  parentId?: string;
}

export interface SystemCountryOptionsType {
  id: string;
  name: string;
  code: string;
  localName: string;
}
