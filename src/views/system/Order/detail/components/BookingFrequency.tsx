import { memo } from 'react'

interface BookingFrequencyProps {}

const BookingFrequency: React.FC<BookingFrequencyProps> = memo(() => {
  return <div className="">BookingFrequecy</div>
})

export default BookingFrequency
