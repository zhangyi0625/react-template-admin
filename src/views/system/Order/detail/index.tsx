import { memo } from 'react'

interface OrderDetailProps {}

const OrderDetail: React.FC<OrderDetailProps> = memo((props) => {
  return <div className="">订单详情test</div>
})

export default OrderDetail
